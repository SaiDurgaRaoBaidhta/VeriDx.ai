# Backend API URL. Used at build time by the Vite frontend.
# - Local: run `npm run server` and set to http://localhost:3001 (or your server port)
# - Vercel: set this in Vercel → Settings → Environment Variables to your deployed backend URL (e.g. https://your-app.up.railway.app)
# Leave empty to use localStorage only (no shared data across users or devices).
VITE_API_URL=
